<?xml version="1.0" encoding="utf-8"?>
<!-- FILE: fragment_mixer.xml — STEP 3: ONE KNOB ONLY -->
<!-- VERSION: 1.0.73 — GAIN KNOB UNCOMMENTED ✅ -->
<!-- UPDATED: 2026-09-13 -->

<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#080810"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🎚️ MIXER — STEP 3: 1 KNOB"
            android:textColor="#40E0D0"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="20dp"/>

        <!-- ============================================== -->
        <!-- STEP 3 — ONLY GAIN KNOB UNCOMMENTED -->
        <!-- ============================================== -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="12dp"
            android:gravity="center_vertical"
            android:background="#12121F"
            android:layout_marginBottom="12dp">

            <TextView
                android:layout_width="90dp"
                android:layout_height="wrap_content"
                android:text="GAIN"
                android:textColor="#AAAAAA"
                android:textSize="14sp"
                android:textStyle="bold"/>

            <!-- ✅ UNCOMMENTED — GAIN KNOB -->
            <ImageView
                android:id="@+id/knob_gain"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:src="@drawable/ic_knob"
                android:layout_marginStart="8dp"/>

            <TextView
                android:id="@+id/val_gain"
                android:layout_width="60dp"
                android:layout_height="wrap_content"
                android:text="0 dB"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:gravity="end"
                android:layout_marginStart="8dp"/>
        </LinearLayout>

        <!-- ALL OTHER KNOBS — STILL COMMENTED OUT -->
        <!-- BASS — LOWMID — MID — HIGHMID — TREBLE — PRESENCE — REVERB — DELAY — COMPRESSOR — PAN — VOLUME -->
        <!-- SLIDERS — STILL COMMENTED -->
        <!-- BUTTONS — STILL COMMENTED -->

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Iba pang element — nakakomento pa rin"
            android:textColor="#666666"
            android:textSize="12sp"
            android:layout_marginTop="20dp"/>

    </LinearLayout>

</ScrollView>
