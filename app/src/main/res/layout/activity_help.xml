<?xml version="1.0" encoding="utf-8"?>
<!-- ================================================== -->
<!-- FILE: activity_help.xml — ✅ KUMPLETONG GABAY + SIDE MENU! -->
<!-- VERSION: 1.0.0 — HANDANG GAMITIN! -->
<!-- UPDATED: 2026-09-16 -->
<!-- ================================================== -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#081218">

    <!-- ✅ PANGUNAHING LAMAN — HELP TEXT -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- ✅ VERSION HEADER — KUSANG NAGBABAGO! -->
            <TextView
                android:id="@+id/tv_help_version"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Version —"
                android:textSize="14sp"
                android:textColor="#888888"
                android:gravity="center"
                android:layout_marginBottom="16dp"/>

            <!-- ✅ BUONG HELP TEXT -->
            <TextView
                android:id="@+id/tv_help_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="#E0E0E0"
                android:lineSpacingExtra="4sp"
                android:fontFamily="monospace"/>

        </LinearLayout>
    </ScrollView>

    <!-- ✅ SIDE MENU — TUGMA SA IBA PANG SCREEN! -->
    <include
        layout="@layout/side_menu_panel"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"/>

</androidx.drawerlayout.widget.DrawerLayout>
